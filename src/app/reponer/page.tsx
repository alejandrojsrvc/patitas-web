import type { Metadata } from "next";
import { BellRinging, Calculator, ShoppingBagOpen } from "@phosphor-icons/react/ssr";
import Link from "next/link";
import { SiteFooter } from "@/components/layout/site-footer";
import { SiteHeader } from "@/components/layout/site-header";
export const metadata: Metadata = { title: "Reponer alimento antes de que se termine | Patitas", description: "Calculá cuánto dura lo que consume tu mascota y elegí si querés recibir un recordatorio.", alternates: { canonical: "/reponer" } };
export default function ReplenishPage() { const steps = [[ShoppingBagOpen, "Comprá normalmente", "No necesitás una suscripción para usar Patitas."], [Calculator, "Calculamos la duración", "Usamos la tabla del fabricante cuando está disponible."], [BellRinging, "Elegí si querés un aviso", "La recompra sigue bajo tu control."]] as const; return <><SiteHeader /><main id="contenido"><section className="bg-brand-blue py-16 text-white sm:py-24"><div className="container-shell"><h1 className="display-heading max-w-5xl text-5xl sm:text-7xl">Reponer no debería empezar eligiendo una frecuencia a ciegas.</h1><p className="mt-6 max-w-2xl text-xl leading-8 text-white/75">Primero entendemos cuánto debería durar lo que consume tu mascota. Después vos decidís si querés un recordatorio o volver a comprar.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link href="/calculadora-alimento" className="inline-flex min-h-14 items-center justify-center rounded-xl bg-brand-yellow px-6 font-semibold text-ink">Calcular una reposición</Link><Link href="/perros" className="inline-flex min-h-14 items-center justify-center rounded-xl border border-white/35 px-6 font-semibold text-white hover:bg-white/10">Ir al catálogo</Link></div></div></section><section className="bg-cream py-16 sm:py-20"><div className="container-shell grid gap-8 md:grid-cols-3">{steps.map(([Icon, title, description]) => <div key={title} className="border-t border-border pt-6"><Icon size={32} weight="duotone" className="text-brand-blue" /><h2 className="mt-8 font-display text-3xl font-semibold">{title}</h2><p className="mt-3 text-muted">{description}</p></div>)}</div></section><section className="bg-white py-16 sm:py-20"><div className="container-shell grid gap-10 lg:grid-cols-2"><div><h2 className="display-heading text-4xl sm:text-5xl">Sin permanencia rígida.</h2><p className="mt-5 max-w-xl text-lg text-muted">El MVP empieza con cálculo, recordatorio por email y recompra manual. Pausar, adelantar o automatizar llegará después de aprender del consumo real.</p></div><div className="rounded-2xl bg-soft-blue p-7"><p className="text-sm text-muted">Ejemplo después de comprar</p><p className="mt-4 font-display text-2xl font-semibold">Compraste el 21 de agosto</p><dl className="mt-6 grid gap-4"><div className="flex justify-between border-b border-border pb-3"><dt>Duración estimada</dt><dd className="font-semibold">39 días</dd></div><div className="flex justify-between border-b border-border pb-3"><dt>Próxima reposición</dt><dd className="font-semibold">~29 de septiembre</dd></div></dl><p className="mt-4 text-xs text-muted">Ejemplo ilustrativo; el resultado real informa la fuente utilizada.</p></div></div></section></main><SiteFooter /></>; }
