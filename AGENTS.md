Este proyecto usa pnpm.

No uses npm ni yarn.

La configuración local está en `.env.local`.

No ejecutes automáticamente build, lint, tests o typecheck.

No ejecutes Prisma migrations ni `prisma db push`
salvo que se solicite explícitamente.